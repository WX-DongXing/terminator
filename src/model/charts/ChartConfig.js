export default class ChartConfig {
  constructor(
    type, container, width, height, padding,
    background, plotBackground, forceFit, data,
  ) {
    this.type = type;
    this.container = container;
    this.width = width;
    this.height = height;
    this.padding = padding;
    this.background = background;
    this.plotBackground = plotBackground;
    this.forceFit = forceFit;
    this.forceFit = forceFit;
    this.data = data;
  }
}
